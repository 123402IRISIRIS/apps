{

  "name": "your-app-name",
  "version": "0.1.0",
  "public": true,
  "homepage": "https://www.irisnicolaeonea.com"

  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
